# Stellar network: testnet or mainnet
STELLAR_NETWORK=testnet

# Stellar CLI identity name (created with: stellar keys generate deployer)
STELLAR_IDENTITY=deployer

# Deployed contract ID (set after running deploy.sh)
CONTRACT_ID=

# Horizon URL
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
